[package]
name = "txt-timer"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
chrono = "0.4.43"
colored = "3.1"
regex = { version = "1.12", default-features = false, features = ["std", "perf"]}
itertools = "0.14"
signal-hook = "0.4.4"

[dev-dependencies]
assert_fs = "1.1"
assert_cmd = "2.1"
predicates = "3.1"

[profile.release]
opt-level = 'z'
lto = true
codegen-units = 1
strip = true
